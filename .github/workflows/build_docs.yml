# This is a basic workflow to help you get started with Actions

name: "Builds docs"

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main_dev" branch
  push:
    branches: [ "main_dev" ]
  pull_request:
    branches: [ "main_dev" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  docs:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:

      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      - name: Set up Python 3.9
        uses: actions/setup-python@v2
        with:
            python-version: 3.9

      - name: Install jumeg conda env
        run: |
            # $CONDA is an environment variable pointing to the root of the miniconda directory
            $CONDA/bin/conda env update --file jumeg.yml --name base

      - name: Build sphinx docs
        uses: ammaraskar/sphinx-action@master
        with:
            working-directory: ${{ env.GITHUB_WORKSPACE }}
            docs-folder: "docs/"
            # pre-build-command: "pip install -r requirements_docs.txt && pip install -e ."

